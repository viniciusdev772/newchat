<!-- No arquivo chamados.ejs -->
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Chamados</title>
    <!-- Importe o CDN do Tailwind CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
</head>

<body class="bg-gray-100 p-8">
    <h1 class="text-2xl font-bold mb-4">Lista de Chamados</h1>
    <table class="min-w-full bg-white border border-gray-300 shadow-sm rounded-md overflow-hidden">
        <thead class="bg-gray-100">
            <tr>
                <th class="py-2 px-4 border-b">Email</th>
                <th class="py-2 px-4 border-b">Solicitação</th>
                <th class="py-2 px-4 border-b">Código</th>
                <th class="py-2 px-4 border-b">Status</th>
            </tr>
        </thead>
        <tbody>
            <% chamados.forEach(chamado => { %>
            <tr class="hover:bg-gray-50 transition duration-150">
                <td class="py-2 px-4 border-b"><%= chamado.email %></td>
                <td class="py-2 px-4 border-b"><%= chamado.solicitacao %></td>
                <td class="py-2 px-4 border-b"><%= chamado.codigo %></td>
                <td class="py-2 px-4 border-b"><%= chamado.status %></td>
            </tr>
            <% }); %>
        </tbody>
    </table>
</body>

</html>